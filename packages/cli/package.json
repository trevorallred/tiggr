{
  "name": "@tiggr/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "tiggr": "./build/cli.js"
  },
  "exports": {
    ".": {
      "types": "./build/cli.d.ts",
      "import": "./build/cli.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf build && rm -f tsconfig.tsbuildinfo",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "tiggr": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.0"
  }
}
